/// Limits the type of visual media shown by the visual media picker.
public enum VisualMediaPickerType {
    
    /// Pick all available visual media types.
    case all
    
    /// Pick only still photos.
    case photo
    
    /// Pick only videos.
    case video
}
